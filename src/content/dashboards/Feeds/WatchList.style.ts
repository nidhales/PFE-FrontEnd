import { styled } from '@mui/material/styles';
import { Paper } from '@mui/material';

export const SolutionBox = styled(Paper)(() => ({
  margin: '20px 0',
  padding: '5px',
  borderRadius: '5px'
}));
